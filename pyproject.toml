[project]
name = "route-optimizer"
version = "0.1.0"
description = "A graph-based route optimisation and pathfinding project."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]